# Backend API (obbligatorio in dev se non usi il proxy Vite su /api)
VITE_API_URL=http://localhost:3000

# Target del proxy Vite (opzionale, default localhost:3000)
# VITE_API_PROXY=http://localhost:3000
